machine:
  node:
    version: 0.12
  timezone:
    America/New_York

  environment:
    appname: tesla

  hosts:
    dev-tesla.reisys.com:  127.0.0.1
    uat-tesla.reisys.com:  127.0.0.1
    tesla.reisys.com:      127.0.0.1

  services:
    - elasticsearch
    - mongodb
    - docker

general:  
  artifacts:
    - artifacts/testing

dependencies:
  pre:
  - npm install -g bower grunt-cli
  - gem install sass
  - gem install compass

test:
   override:
      - sh ./application/run-tests2.sh
